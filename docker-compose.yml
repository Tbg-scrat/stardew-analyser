
services:
  sv-analyzer:
    build: .
    image: stardew-analyzer:latest
    container_name: stardew-analyzer_dev
    ports:
      - "9999:80"
    environment:
      - SAVE_DIR=/saves
      - SAVE_NAME=Friisen_433608217
    volumes:
      - /srv/docker/data/sv-analyzer/saves:/saves:ro
    restart: unless-stopped
